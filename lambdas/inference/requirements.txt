# Must match the model.joblib training environment EXACTLY (issue #4) so joblib can
# unpickle ml.training.model.HybridModel. pyarrow added for reading the ETL Parquet.
scikit-learn==1.6.1
numpy==2.1.3
pandas==2.2.3
joblib==1.4.2
pyarrow==18.1.0
